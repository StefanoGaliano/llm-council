# Claude model access (required)
ANTHROPIC_API_KEY=

# Primary web search, free tier (required)
TAVILY_API_KEY=

# Fallback web search, free tier (optional)
BRAVE_API_KEY=

# GitHub repo signals — read-only PAT (recommended)
GITHUB_TOKEN=

# Verbose logging when set to 1 (optional)
COUNCIL_DEBUG=
